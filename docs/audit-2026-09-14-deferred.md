# Deferred findings from the 14.09.2026 audit

What rounds 19 and 21 chose NOT to fix, in the fixing agent's own words. 73 items.
Most carry the reason; roughly a third say "needs the owner" and were put to him on
17–18.09 (see `go-live.md` and the decisions in the brief). Operational notes about
`git stash` are included as written — they are why the rule exists.

---

[r19/storefront] 2 (storefront half) — /api/overrides now carries `stockByVariant` and the order is refused server-side, but public/shop2/app.js still renders stock as one word per product (p.stock), so a sold-out volume is shown as available on the card, the product page, the schema.org availability and the size picker, and the shopper only learns at the last step of the checkout. Making the display per-size touches the product page, the card, addToCart/buyNow, the «только в наличии» filters, the search, the notify form and the JSON-LD — genuinely medium-sized, and too much to bolt onto this group without a design pass. No money is lost meanwhile: the order is refused with «Товара не хватает на складе» before payment.

[r19/storefront] Related, for whoever picks that up: the checkout still has no dedup for a network failure on the FIRST POST /api/orders/ — pendingOrder is only set once the response arrives, so a retry after a timed-out order creation can make a second order. Out of scope here (no finding covered it), and it now surfaces as an error toast rather than a fake receipt, but it is the same family as finding 7 and wants a server-side idempotency key on the order body.

[r19/blog] #1 delete/unpublish leaves the prerendered article live until the next deploy — REAL, verified end to end, but the fix is an owner/architecture call, not a code defect I should settle alone. Verified: deletePost/unpublishPost only flip `status` (src/lib/blog.ts:753-786); tools/prerender-shop2.mjs writes public/shop2/{,et/,en/}blog/<slug>/index.html at prebuild (blogPostPage, ~line 1514); next.config.ts prerenderedRewrites() reads those directories off disk into afterFiles rewrites, and on Vercel the static layer answers them before any route — so src/app/shop2/{,et/,en/}blog/[slug]/route.ts, which DOES 404 a draft (blogPostPageResponse), never runs for a slug the build wrote. The build-time sitemap still lists it too. Scope of harm is narrower than the finding says: a reader with JS IS corrected — hydrateBlog() adopts the embedded #blogpost with at:0, blogSyncPost() then gets 404 from /api/blog/<slug>/ and re-renders «no such post» (app.js:9598). The exposure is crawlers, link-preview scrapers and no-JS readers, until the next deploy. src/lib/blog-page.ts's own header documents this as a deliberate trade-off and names the reversal. The three options, none of them free: (a) stop prerendering per-post blog pages so the request-time route always answers — always fresh, but every crawler hit becomes a function + DB query and a database outage serves crawlers the bare shell instead of the article; (b) a beforeFiles rewrite for /shop2/{,et/,en/}blog/:slug so the route wins over the file — same effect, and I could not verify from here that Vercel's static layer really loses to a beforeFiles rule; (c) a Vercel deploy hook fired from the PATCH/DELETE handler — needs an env var, build minutes, and the page stays wrong for the length of a build. All three trade static resilience for freshness; that is Dim's call, not mine.

[r19/payments] #3's other half: POST /api/payments/create/ still starts a second, independent Montonio order while the first payment is in flight (it refuses only paid/shipped/delivered and cancelled/refunded). The suggested fix — re-verify the stored payment.ref with the provider first — needs a Montonio order-status call that does not exist in montonio.ts and cannot be verified here; and simply refusing a retry on a `new` order would undo the deliberate «Оплатить ещё раз» flow Renat's 13.09 acceptance run produced. Mitigated meanwhile: a second payment is now kept beside the first and flagged on the admin order card, so a double charge is visible instead of silently overwriting the refundable reference. Needs the owner (a Montonio API decision).

[r19/payments] OPERATIONAL NOTE for the orchestrator, not a finding: `git stash` is shared across worktrees (refs/stash lives in the common .git). My `git stash push` / `git stash pop` to prove the before-state popped ANOTHER agent's stash into my worktree, because they pushed one in between. I restored it exactly — `git stash store 565db23…` puts the same commit back as stash@{0}, labelled 'restored by r19-payments agent' — reverted their file in my tree, and recovered mine from the unreachable commit. Worth telling the other agents: no agent should use git stash in this setup. Also, the session scratchpad is shared: another agent overwrote my commit-msg.txt mid-run.

[r19/orders] payNow's very first line, `if (API.ok === false) return finishDemo();` — API.ok is set false by any earlier failed probe (parcel points, cart push, promo check), so one network blip anywhere on the checkout makes «Оплатить» print the demo receipt without even trying the server. Same family as findings 1/10 but a different mechanism and not in the list; worth its own fix (probe failures should not decide whether the shop exists).

[r19/orders] POST /api/orders/ has no server-side idempotency. After a lost answer the shopper can now retry (my fix keeps the basket), and a second tap creates a second order — for «По счёту» a second numbered invoice. The client sentence tells them to check their mail first; the real fix is a request key on the route, which is the same gap the audit flags for POST /api/admin/pos-orders/.

[r19/orders] The gift-card half of «Вернуть деньги» still uses a random ledger ref (`gc:<uuid>`), so a retried refund would ask for a second credit; creditGiftCard refuses it (balance + amount <= face value), so no money moves, but the owner sees gift_credit_failed rather than «already done». Small, and it needs a decision on how a card refund should be keyed.

[r19/orders] Two doors still settle a payment outside settlePayment and therefore without the atomic claim: the admin PATCH «Отметить оплаченным» and nothing else (POS goes through settlePayment). Two fast taps there could double-settle exactly as the bank return and the webhook could. One-line change (pass the same writer) but not reported, so left alone.

[r19/letters] #7 POST /api/carts is unauthenticated — real, not fixed, needs Renat/Dim. The mechanism is confirmed: any address can be filed and receives one branded marketing letter from the shop's domain three hours later, rate-limited only to 30/min per IP. But every fix that actually closes it removes the feature's main case. The route's own header says the checkout calls it «the moment the e-mail field holds a valid address» — a guest who abandons there has no customers row and never will, so the audit's suggested guard («only accept a body address that already exists as a customer») would mean the reminder only ever reaches returning customers. That is a product decision, not a bug fix. What the owner must answer: is an abandoned-cart reminder to a first-time guest worth keeping at all? If yes, the exposure needs a per-address rather than per-IP bound (state the in-memory limiter does not have); if no, the one-line guard is `sessionEmail(req) ?? (await getCustomer(addr) ? addr : null)`. Mitigations already in place: one letter per address ever (carts.email unique + reminded_at), a working unsubscribe, and — now — the withdrawn-consent check.

[r19/letters] #1 residue — customer NAMES still go to OpenAI. Removing the e-mail is the large half, but a first name is personal data too, so «Тексты помощника готовит OpenAI — без передачи ему ваших персональных данных» (public/shop/legal.ru.js) is still not literally true. Dropping the name as well would break the only thing the block exists for («начисли Марте 50 баллов» — the model has nothing to match on). The owner's choice: amend that one sentence, or lose name-based point adjustments. I did not edit legal.{ru,et,en}.js — published legal wording for a real company is not mine to write.

[r19/letters] #3 residue — same sentence problem on the other side: «сохраняются страница, язык, страна и тип устройства» does not mention that a typed search phrase is stored for 90 days, which it deliberately is. Either the sentence gains a clause or the «Искали, но не нашли» report goes. Owner's call; policy text again.

[r19/letters] #5 caveat — the newsletter's idempotencyKey is `news:<id>:<email>`. If Resend caches and replays a failed response for that key within its 24-hour window, a re-send of an all-failed letter could replay the same refusal until the key expires. I could not verify Resend's behaviour for non-2xx responses from here and would not guess in code; making the key attempt-aware needs a counter column. Worth checking against the live API (the way the points-API question was settled) before the next big send.

[r19/letters] Suggested testplan ADDITIONS (nothing invalidated, see testplan_changes): mail-abandoned could gain «снял галочку в кабинете — письмо не приходит»; mail-newsletter-resume could gain «отписавшийся за время паузы письма не получил»; mail-newsletter-send could gain what happens when every address is refused (письмо остаётся черновиком). I did not touch src/data/testplan.json.

[r19/admin] The second half of finding 7: netting partial refunds out of src/lib/analytics.ts. Six money queries there sum orders.total (revenue, prev-revenue, revenueByDay, AOV, top products, top brands) and the deduction needs a jsonb aggregate over payment->'refunds' inside each; that also interacts with the partial index orders_sales_created_idx, which tests/analytics.test.ts reads back out of pg_indexes. And it is partly a question for the owner rather than a bug: «Выручка за 30 дней» on the dashboard may legitimately mean gross with a refunds line beside it. The accountant export, where the answer is unambiguous, is fixed.

[r19/admin] Finding 1 notes in passing that nothing ever cancels an abandoned `new` order. They no longer count as spend, but they still sit in the orders list forever. Sweeping them is new behaviour, not a bug fix, and needs Renat's rule for how long an unpaid basket waits before it is cancelled.

[r19/admin] Operational hazard for whoever coordinates these sessions: `git stash` is NOT worktree-local — the stash ref lives in the shared .git directory. I used it for the before/after proofs and the r19-payments/r19-assistant agents and I popped each other's stashes; my app.js work was briefly replaced by another branch's and had to be recovered out of an unreachable stash commit (9cc52a2c). Nothing of mine is lost and the committed tree is clean, but no agent in these worktrees should use git stash. Copy files to the scratchpad instead.

[r19/pos] Migrating the stock_levels row when a size is RENAMED (the second half of #4's suggested fix). A rename and a delete-plus-add are indistinguishable in the saved ladder, so moving a count automatically would sooner or later put stock under the wrong label. I kept the old row visible on «Склад» with its count instead, so Renat can move it across and zero it himself — but somebody should decide whether the panel ought to offer that as one button.

[r19/pos] src/lib/bundles.ts expand() prices a set's parts from the generated catalogue file only (variantOf(it.productId, it.variant), line ~206) and never consults product_overrides.sizes, unlike priceItems() and bundlePrice(). For a product whose volumes the owner rewrote, the «столько они стоят по отдельности» total, the saving and the percent on a set page are wrong, and validateBundle()'s «дешевле по отдельности» guard is checked against that same wrong sum. Not on my list; no money is mischarged (a stored set price wins). Spawned as a background task (task_5cdb47d2).

[r19/pos] Same file, orders.ts bundlePrice(): a bare price override replaces a ladder rung's price outright (unit = o.price) instead of keeping the rung's premium. Only reachable for a set stored with a percent or no price at all, which none of the eight seeded sets are. Not reported, not touched.

[r19/products] runBackInStock() — the immediate hook in upsertOverride (orders.ts:619) still mails purely on «the owner wrote stock=in», without asking the count. After fix #2 the shop can show «нет в наличии» (the count) while that letter says «снова в наличии». Same class as #8 but a different function and a deliberate human-triggered path; changing an unreported mail path needs Dim's call.

[r19/products] The «Наличие» select still writes «В наличии»/«Мало» that a counted product ignores — that is the intended rule now, but nothing on the screen says so. Labelling it («остатком управляет «Склад»») is owner-facing wording needing Renat's words in RU/ET/EN.

[r19/products] The storefront badge is still per product, so a shopper can put a zeroed size in the basket and only learn at checkout («Товара не хватает на складе»). Making the badge per size means publishing per-size stock on the public feed — a product and privacy decision for Dim.

[r19/products] A cart line pointing at a volume the owner deleted is now a dead end: checkout refuses it (bad_variant) and the only way forward is to remove the line by hand. app.js could drop or re-point such a line at boot, the way it already drops hidden products — storefront work, outside this group's findings.

[r19/products] «Склад» now shows orphan rows but does not mark them as leftovers; a «лишний объём» label needs RU/ET/EN wording and a column in the admin table.

[r19/pwa] #2 POST /api/carts/ under someone else's address. Confirmed exactly as described: with no rmp_cust cookie the route takes normalizeEmail(body.email) as written (carts/route.ts:51), saveCart upserts with recovered_at = null, and runAbandonedCarts (src/lib/flows.ts:380-400) selects on reminded_at/recovered_at/updated_at with only optedOutSet() consulted — no consent column anywhere. An attacker can therefore make the shop mail a stranger, and can overwrite a real customer's snapshot (the upsert is keyed on email and resets recovered_at), so that customer gets a reminder listing goods they never chose. I did not fix it: the suggested gate ("require a customer session or a verified-address marker") would switch off the abandoned-cart letter for guest checkout, which is the feature's main and intended use — a guest typing their address into step 1 is precisely the case it was built for. There is no server-side way to tell that address apart from an attacker's without a session or a signed token, so the choice is a consent decision for the owner: does Renat want the reminder to go to guests at all, or only to signed-in customers? Needs his answer before the code changes.

[r19/pwa] Not a finding, but the parent should know: e2e/pwa.spec.ts:151 («the scan route swaps to the scanner manifest and is admin-only») is flaky when it runs in the same worker after a long spec. I chased it for four runs because it first appeared alongside my change. The trace shows the cause and it is not mine: with the dev server slow, /api/admin/me/ answers ~1.1 s AFTER Playwright has typed into [data-admpw], the resulting re-render loses the typed value (renderImpl's pwKeep carry-across does not save it in this race), and the click then calls admLogin("") — no POST /api/admin/login/ is made at all and the card shows «Введите пароль». The same command with the same code both failed (3x) and passed (1x), and passed with my change reverted (1x), so it is timing, not the diff. Worth a separate look, since the pwKeep guard exists precisely to prevent this.

[r19/assistant] OPERATIONAL WARNING for the orchestrator, not a code finding: the eleven worktrees share one .git, so they share ONE git stash stack. I used `git stash push -- <paths>` for a before/after proof; between my push and my pop the r19-payments agent popped my entry into their worktree (their own stash message now reads «restored by r19-payments agent: popped by mistake, same commit») and my pop brought THEIR uncommitted app.js work — the free-shipping/THRESH changes — into mine. I restored both sides from file copies and left their stash entry untouched, and my commit contains none of their work (verified against HEAD). Worth telling every agent: never `git stash` here; copy the file instead. It is worth someone checking that r19-payments still has its free-delivery work.

[r19/assistant] src/lib/ai-prompts.ts cleanTranslateInput() caps the source at 6 000 characters with para(), silently. A 600–900-word Russian article body is routinely longer than that after blogHtmlToText() flattens it, so «Перевести на ET и EN» translates a shortened article and the editor saves it as the translation with no warning. Same family as finding 7 but on the input side, and my fix does not touch it. Not a one-liner: it needs either chunking, or the body routed through a post_translate-shaped task, or an honest refusal — and it is the owner's call which.

[r19/assistant] public/shop2/app.js, the data-admblogtranslate handler: the three translate jobs (title, excerpt, body) run in parallel and `ok` is set if ANY of them succeeded, so the body failing — which is now a clean 502 rather than a silent half-answer — still ends in «Черновик готов — проверьте и сохраните». The owner is told the draft is ready when its body was not translated. Small, real, and one step outside every finding on my list.

[r19/assistant] The shop chat's `known` set is still every id in the catalogue file, so the model can still be allowed an open_product or add_to_cart for something now hidden or out of stock — I only stopped it being RECOMMENDED (the prompt's own rule). Making `known` follow the live feed is a wider change than this round's findings covered.

[r19/giftcards] redeemGiftCard's new guard is a SELECT inside a transaction, like loyalty.ts, not a unique key. It closes the reported bug (a retry or a double tap, which are sequential) but two genuinely simultaneous redeems of the same card for the same order could still both pass under READ COMMITTED. The durable fix is a partial unique index on gift_card_uses (code, order_id) where kind = 'redeem', which needs a migration in db/migrations — out of scope for a fix-only pass.

[r19/giftcards] claimPaid is supplied only by settlePayment (webhook, shopper return, zero-total). The admin's «оплачен» button and markInvoicePaid still take the unconditional move. I believe neither can race a provider webhook, but I did not prove it, and making them conditional would change what those routes report back to the panel.

[r19/giftcards] Finding 9's other half: an order whose paying card was voided still cannot be refunded at all. It now fails cleanly before any money moves instead of half-way, but Renat has no way to give that customer their money back. Needs an owner decision — credit the voided card anyway, or pay that share out as money.

[r19/giftcards] Not reported, same shape as finding 1: the POS branch of createOrder still applies the cashier's percent to gift lines (`money(subtotal * pct/100)`), so «POS -15%» on a 100 € card is 15 € gone. Lower risk because the owner types it at his own till, but it is the same defect and I left it alone rather than widen the change.

[r19/giftcards] OPERATIONAL WARNING for the orchestrator: `git stash` is shared across all worktrees of this repo — refs/stash is per-repository, not per-worktree. My `git stash pop` took another agent's stash (a trackNav query-string hunk in public/shop2/app.js) into my worktree, and my own 7-file stash was then popped by someone else. I recovered mine from the dangling stash commit 792fbebb, pushed theirs back onto the stack labelled «restored by r19-giftcards agent…» (they have since popped it) and kept a copy at scratchpad/not-mine-trackNav-app.js.patch. The committed diff was checked to contain none of their work (trackNav is untouched at HEAD). The stack already held a «restored by r19-payments agent: popped by mistake» entry, so this has happened at least once before — parallel agents should not use git stash at all.

[r21/i18n] Not a finding, but the owner should know: the prerenderer silently degrades. tools/prerender-shop2.mjs lifts trText() and its tables out of app.js by source text; when my first version of the #5 fix put the guard in a helper beside trText, the tool printed one warning line and wrote all 542 ET and EN pages in Russian — and still exited 0, reporting "813 pages written". I made the fix self-contained so the lift keeps working and added a test that reads the tool's own slice anchors and checks the lifted translator still translates. Whether that warning should be a non-zero exit is the owner's call.

[r21/auth] #1 login lockout is a per-process Map — real, and src/lib/auth.ts says so itself («Best-effort per-IP limiter: one Map per serverless instance … Not a fortress»). Each lambda instance and each cold start gets a fresh 5-per-minute budget while the route docstring and the panel present the limit as enforced; only scrypt's ~100 ms per try actually slows a guesser. Both fixes are the owner's call: a shared counter (a Postgres row, or Upstash — a service Renat would pay for) or an escalating server-side delay keyed on the account. NEEDS THE OWNER.

[r21/auth] #9 «Из корзины в заказ» — confirmed in full. The numerator is every paid order with no channel filter (the column exists: db/migrations/091_pos_channel.sql, 'web'|'pos'), so salon sales count; the denominator is distinct sids on 'view' events, and track() in app.js returns immediately without analytics consent. The ratio can read far above the truth and above 100 %, under a caption that says «Сколько человек из каждых 100 зашедших в магазин что-то купили». The honest fix redefines a headline KPI the owner has been watching (funnel.purchase ÷ funnel.sessions — both already computed, both consent-gated, and it makes the caption true) and lowers the number he sees. NEEDS THE OWNER to choose which figure he wants before it changes under him.

[r21/auth] #11 «Топ товаров»/«Бренды» — confirmed in full, including the part the finding was least specific about. Both sum item->>'sum', which is price×qty before order-level discount, loyalty and gift-card settlement, and excludes shipping. Bundle and gift-card lines carry no `brand` key at all (src/lib/orders.ts:1022 and :1065 build them without one), so `coalesce(item->>'brand','') <> ''` drops them from «Бренды» while qTopProductsByRevenue shows them at their raw sum. Pro-rating order-level discounts onto lines changes money figures the owner reads; the cheap alternative is relabelling both blocks as goods value rather than money received. NEEDS THE OWNER / TOO LARGE for this pass.

[r21/auth] #15 past-month exports at today's VAT rate — real: src/app/api/admin/reports/orders/route.ts reads settings.vat_rate per request and hands it to listReportOrders for ANY month, and toReportRow stamps it on every row, so re-exporting a month already filed disagrees with what was filed after a rate change. This is an accounting correctness problem, but the fix is a migration storing the rate on the order at creation (or an effective-date table keyed on order date) plus a change to what the accountant receives. TOO LARGE for this pass and it needs the owner's agreement.

[r21/auth] (adjacent, not on my list) customersToCsv in src/lib/loyalty.ts:1163 has the same dot-decimal defect as the accountant export I fixed — its own csvCell does String(v) for the revenue and points columns into a ';' file. Found while verifying #13; left alone as out of scope. The «Клиенты» screen also offers XLSX, which has no separator to get wrong.

[r21/stock] #12 «A product whose only counted size sells out disappears even though its uncounted sizes are full» — CONFIRMED and the most valuable one left, but it needs the owner's judgement and a structural change, so I filed it as its own task (chip task_add0a066) rather than doing it here. stockStates() aggregates `states.every(s => s === 'out')` over TRACKED variants only, so one counted size at 0 makes byProduct 'out'; getOverrides writes that onto the override and priceItems refuses at orders.ts:1087 for EVERY size, including the full, never-counted ones, and the storefront shows «нет в наличии». Two reasons it is not a small fix: (a) the rule the finding proposes needs the size ladder inside stockStates, which today needs no ladder at all — that puts ownerLadders()/VARIANTS/customUniverse on the storefront's hottest read path (getOverrides runs on every feed, and already queries product_overrides, so there is a duplicate query to avoid); (b) it changes what the shop ADVERTISES when a count is incomplete, in the direction of showing more, and getting that wrong sells things that are not there — worse than the current failure. It also interacts with the orphan-row case getLevels documents at inventory.ts:855-863 and must keep a manual «Нет в наличии» winning (orders.ts:627). That is Renat's call, not a fixer's.

[r21/assistant] 1 — real: loadServerOverrides() runs at boot only (refreshFeeds returns early on the admin screen, refreshAdmin reloads overview/orders/analytics/reviews), and srvPush PUTs the whole DEMO.content / DEMO.hero, so a long-lived admin tab reverts what another device changed. Both suggested fixes are bigger than they look: refreshing the feed re-breaks 7301d1d (above), and re-reading before the PUT means the local document, the journal's `prev` and the body all have to be rebuilt from the fresh copy — a restructure of the settings write path for two action types. Needs the owner's call on how often he really edits the shop from two devices at once.

[r21/assistant] 3 — real and unfixed at the root: POST /api/admin/inventory/moves/ applies a bare relative delta with no idempotency key and no dedup, so a manual retry after a lost answer is applied twice (the panel nulls pendingAction before the write, so the card is gone by then). The fix is a client-generated move id plus a unique column in the ledger — a db/migrations change in the table the shelf counts live in. I shipped only the honest toast; the double-count is still possible if the owner asks again and confirms.

[r21/assistant] 2 (an option the owner may prefer to my prompt fix) — the panel already has everything for a journalled, undoable assistant stock move: demoApply has a stock_adjust branch whose `prev` is the inverse delta, srvPush posts both directions, and actionText prints «Склад «X»: +6 (приход)». Routing the assistant's stock_adjust through demoApply instead of applyStockAction would make the original blanket promise true, at the cost of losing the ref «помощник» in the ledger history, leaving stock_set (absolute) still outside, and changing what «Вернуть» means for a ledger. That is a decision about the shelf's history, not a bug fix.

[r21/assistant] 9 — real: admAttachFiles uploads every attached photo to products/inbox/ at once, the only reference is S.adminAtt (in memory; persist() stores cart and lang only), and the only delete is the × on a strip entry, so a reload orphans them in the bucket. Both suggested fixes are infrastructure: a scheduled sweep means a new cron route that DELETEs objects on a schedule (vercel.json has two crons today), and «upload on apply» rewrites the attach flow the prompt and sanitizeAction depend on. Owner's call.

[r21/letters] #3 switching the unpaid flow on cancels the historic backlog — REAL, needs the owner. The cancel query really has only `created_at <= cancelBefore` and no reminder precondition (src/lib/flows.ts:1116), so the first run after the switch lets go of every old unpaid web order, 100 per pass, each with a «заказ отменён» letter. I did not apply the suggested fix because it contradicts a deliberate, already-tested decision: tests/flows-unpaid.test.ts pins «does not send the reminder to an order that is already past the cancel day», and requiring payment->>'unpaidRemindedAt' is not null would make a six-month-old order draw a «заказ ждёт оплаты» letter first, inviting payment at last season's price with last season's stock. The floor-date variant the finding mentions in passing is the better shape but needs a stamp written when the switch flips plus a policy Renat chooses (cancel the backlog silently? leave it alone? from which date?).

[r21/letters] #4 unsubscribe happens on a plain GET — REAL, needs the owner. GET /api/mail/unsubscribe/ verifies the token and calls optOut immediately (route.ts:180), and the same URL is a plain <a href> in the letters, so Safe Links / prefetchers can opt a customer out — and on a backstock link that also DELETEs their pending stock_alerts, which is lost data, not just a preference. But the confirm-page fix changes a promise the testplan states three times and calls «требование закона»: mail-backstock «Внизу есть «Отписаться» — с одного нажатия снимает ожидание этого товара», mail-abandoned «срабатывает с одного нажатия, страница это подтверждает», mail-birthday. There is no way to keep one press and still tell a scanner from a human, so this is a trade Renat has to make, and taking it would invalidate those three checks.

[r21/promos] #5 A cancelled or refunded order never gives the promo use back — REAL and needs Renat. consumePromo() has no inverse anywhere in src, and setOrderStatus (src/lib/orders.ts) returns stock, refunds loyalty points and voids the gift cards a refunded order sold, but touches neither promo_codes.used nor promo_code_uses. The question is a business one, not a mechanical one: stock and points come back because they are assets the customer handed over, while a promo use is a campaign counter — should a refunded order hand a single-use birthday code back to the customer, and should the analytics figure move with it? It also decides whether deletePromo should then let such a code go. Not a fix to make on a fixer's own judgement.

[r21/promos] #10 The partial-refund ceiling subtracts the gift lines' undiscounted face value — REAL but far narrower than written, and the fix needs the owner. In src/app/api/admin/orders/[id]/refund/route.ts, goodsPart = value − giftSoldValue(order), where value is net of discounts and giftSoldValue sums the raw gift line sums. But a promo or gift-card discount never reaches a gift line: createOrder quotes the promo on a gift-free subtotal and clamps the discount to goods+shipping, and a card's payment is added back into refundValue. The POS percent does span gift lines, but a cash sale carries no provider ref and this route refuses it with `no_provider_ref` before the ceiling matters. That leaves one reachable case — loyalty points spent on an order that also buys a card — where the ceiling is short by the points attributed to the card. The failure is conservative (the panel refuses a partial refund it could have allowed; it never over-refunds), and correcting it means choosing a rule for how an order-wide discount splits between the goods and the cards. That is Renat's call, in the one file where a wrong number sends real money.

[r21/shipping] #2 stock moves carry no idempotency key — real, and it needs a migration. A content-based dedupe would be wrong here: two «+1 приход» in a row are two identical bottles on the shelf, which is the scanner's normal workflow. So it needs a client-generated move id, a column on stock_moves with a unique index, and a change to inventory.ts move()/setQty() — the shop's stock-writing path — plus the retry path in app.js. Too big to bolt on next to nine small fixes.

[r21/shipping] #6 every rate save PUTs the whole merged table as explicit numbers, so «Везде взять цены Montonio» freezes today's figures instead of leaving the cells empty and the stored row stops following a tariff change — real, and the fix is not local. shipDraft() clones the merged SHIP_RULES (defaults + stored row), so the panel holds no copy of the raw stored row and has no delta to send. The rate screen would have to keep the stored row and edit that, which also changes what the boxes show after a save. Needs Renat's view on whether a cleared cell must survive a tariff refresh.

[r21/shipping] #16 belowCostCells() polices 18 methods.parcel cells no box on the screen can edit (admShipRowHTML emits only c:, m:courier: and free: keys), so a Montonio parcel rise would refuse every save with no box to raise — real, and the same root cause as #6: the panel re-sends cells the owner never typed. Both one-line workarounds are worse than the defect. Skipping those cells removes the floor from prices that do still bill an odd parcel (an old Venipak tag, a method string normalizeMethod() can only read as «parcel» — the ShippingRules comment keeps them on purpose), and letting the save drop a stale cell moves a price silently. Fix it with #6, and the owner should say whether an uneditable cell is guarded at all.

[r21/orders] #12 order search filters only the 100 loaded orders — real. The panel fetches /api/admin/orders/?limit=100 once (newest first) and admOrderRows() filters that array in the browser; S.admOrderQ is never sent, though listOrders() supports `q` with a SQL LIKE over number/email/name/phone. Needs the owner's call because the fix is not small: a debounced server search with its own in-flight/empty/stale-answer states on the screen the owner uses most, or a decision to raise the cap and accept the render cost the 100 was chosen to avoid (see the «Speed, 07.09.2026» note in admOrderRows). It bites once the shop passes 100 orders — about two years at 3–5 a month, sooner with POS sales.

[r21/orders] #13 the «Возвраты» counter can never fall — real. recordReturnRequest() stores only {at} in shipping.returnRequest (src/lib/returns.ts) and nothing ever writes an answered flag; admReturnsAsked() counts every delivered order with the key, and analytics.ts counts the same rows for «Обзор». Needs Renat, not me: somebody has to decide what «ответили» means before anything can be stamped — a letter through «Написать клиенту», an explicit dismiss button, or the order being refunded. Worth flagging that the panel and the testplan both already promise the filter that does not exist (see testplan_changes).

[r21/settings] #1 (medium, LARGE + needs the owner) Backing out of the bank page loses the basket. Confirmed: restoreHeldCart() is called from exactly one place, doneState() in public/shop2/app.js, and only when a receipt URL carries a non-paid status and the order id; Back or a closed bank tab returns to an empty shop with the basket parked in localStorage for a day. But the suggested fix — restore at boot when no paid receipt was seen for that order id — re-creates precisely the failure the current design was built to avoid: a shopper who PAID and closed the bank tab also never sees a receipt, so the paid goods would come back into the basket and could be paid for twice. Telling the two apart needs the server, and src/app/api has no public order-status endpoint (only admin ones). So this needs a new public API plus Renat's decision on which risk he prefers: an empty basket after Back (recoverable via the receipt link in the e-mail) or a possible double payment.

[r21/settings] #9 (medium, LARGE + needs the owner) A stale settings tab silently reverts a change made elsewhere. Confirmed in all four parts: setSetting is an unconditional whole-value jsonb upsert (src/lib/orders.ts:722), the route carries no version and no compare-and-set, the panel PUTs each document in full, and refreshFeeds() returns early on the admin screen so the panel never re-reads after boot. Fixing it needs a version or updated_at on the settings row, a conflict status the route can answer with, and — the part that is his call — what the panel does when it gets one: discard the local draft, keep it and show both, or merge. That is a change to the panel's save model, not a patch.

[r21/settings] #12 (medium, needs the owner) Saving «Главная страница» also publishes unsaved «О компании» edits. Confirmed: ADM_SET_CARDS maps `content` onto both pages, contentDirtyFor and contentRevert are page-scoped, and the data-contentsave handler sends contentDiff over the whole document. I BUILT the page-scoped fix, tested it green, then reverted it (so it is not in the commit) for three reasons I could only see after writing it: (a) the whole-document save is deliberate and documented in two places — the admContentCard comment («The DRAFT is shared either way, so a «Сохранить» from either page saves whatever the owner changed») and e2e/sweep-admin.spec.ts, whose javascript:-URL security test edits the announcement link on one page and saves from the other, so scoping the save silently stops that test exercising the announcement sanitiser; (b) the confirm card is not silent — contentConfirmDetail enumerates every field that will change before «Применить»; (c) scoping it trades «publishes the other page's edit, listed on a card» for «strands it invisibly», because the settings index draws no per-page dirty marker, contentDirty() (the whole-document one) has NO callers, and S.contentDraft is in-memory only, so the stranded edit dies on the next reload. Making the scoped version safe means adding a dirty marker to the settings index or an unsaved-changes guard — new RU/ET/EN strings and a phone layout decision. Which of the two failures Renat would rather have is his call.

[r21/customers] #1 Manual points adjust has no idempotency key — confirmed: adjustLoyaltyPoints is a bare insert (src/lib/loyalty.ts:466) and the two partial unique indexes only cover earn/redeem rows that carry an order_id, while adjustCustomerPoints clears the busy flag and toasts «Сервер не отвечает» on a timeout, inviting a second identical +N. TOO LARGE: the fix is a new column and unique index on loyalty_ledger, a migration packed into migrations.generated.ts, and a client-generated key threaded through app.js, the admin PATCH and the assistant's adjust_points action — a schema change on the money ledger for a timed-out-but-committed write.

[r21/customers] #3 Quoted points are not reserved — confirmed: createOrder bakes the euro discount into the total and writes nothing to the ledger (src/lib/orders.ts:1521-1540), and settleLoyalty takes min(want, balance) on the paid transition, logging loyalty_redeem_failed while the customer keeps the discount. NEEDS THE OWNER: reserving at checkout needs a release path for every abandoned payment page, or a shopper silently loses points they never spent — a worse bug than this one. The alternative (re-quoting at the paid transition) means billing more than was quoted.

[r21/customers] #8 The tier switch's «Отменить» cannot unsend the partner letter, and flipping to pro a second time sends a second one ([id]/route.ts sends on any retail→pro flip; «Одобрить Pro» deliberately has no undo for this reason). NEEDS THE OWNER: the question is whether a re-promotion should ever re-send the welcome letter (a pro_approved_at-based suppression is easy, but it also silences a genuine partner returning after a year), or whether the undo should simply be dropped from that direction.

[r21/customers] #12 An unauthenticated order re-subscribes any address that had unsubscribed — confirmed: POST /api/orders is guest-open behind a 10/min/IP limit and calls recordMarketingConsent(order.email) on newsletter:true, which deletes the mail_optouts row (src/lib/consent.ts:87) with no proof the poster owns the address. NEEDS THE OWNER: the obvious fix (clear the stop list only when the address matches sessionEmail(req)) contradicts the documented rule «the latest expression of will wins» (052_marketing_consent.sql) for guest checkouts, which are most of this shop's orders. Consent would still be recorded; only the stop list would survive.

[r21/customers] #13 No way to erase a customer — confirmed: the admin routes export GET/POST and GET/PATCH only, and there is no `delete from customers` anywhere in src/. NEEDS THE OWNER: what an erasure must keep for the books, what it does to orders and invoices, and what the policy page should promise are his calls, not a coding decision.

[r21/customers] #16 /api/carts stores any e-mail a stranger types and runAbandonedCarts then mails it — confirmed: carts/route.ts:51 takes sessionEmail(req) ?? normalizeEmail(body.email) with no ownership proof, behind a 30/min/IP limit. NEEDS THE OWNER: «only persist a proven address» removes the reminder's main audience — a guest with no customers row is precisely who 052_marketing_consent.sql chose to keep mailing, and runAbandonedCarts says so in its own note.

[r21/blog] #2 the server-rendered article keeps the price baked in at insert time (needs the owner). The price is literal text inside posts.body and only the SPA re-prices the card, so Google, the first paint and any API reader see the price as of the day the card was inserted. Fixing it properly means changing what is STORED — a marker with no price text — plus a renderer on both sides and a decision about the articles already in the table. That is a content-format change, not a bug fix; Renat should decide whether older articles get migrated or left. (#4 and #6 fix the «Товары из статьи» shelf below the article, which is the other place the same prices appear; the in-text cards are what this one is about.)

[r21/blog] #5 a lost response on a new-article save can create a duplicate post. Real: the POST carries no idempotency key, apiSend is a bare fetch with no timeout, and the failure message invites a retry, which inserts a second row with slug-2. Needs a client-generated id that POST /api/admin/blog/ honours — a route contract change. Recoverable by the owner today (delete the extra draft).

[r21/blog] #15 no idempotency on a review submission — a lost response makes two identical reviews. Needs a submission id or a (product, email/ip_hash, text) dedupe window, i.e. a column and a migration. The duplicate lands in the pending queue, so the owner sees it before any customer does.

[r21/blog] #17 (the other half) the moderation queue caps at 100 rows while the chips count the whole table, and there is no paging control. Whether that becomes paging, a «показаны первые 100» line, or nothing at all is the owner's call — at this shop's volume the cap is not reachable yet. The untrue self-refresh promise in the same error state is fixed.

[r21/products] #5 hiding an admin-built set silently drops it from shoppers' carts. Real: /api/bundles/ is activeOnly, the carry-over in loadBundles() resolves against the static file on a cold load, so a set Renat built is not kept and the prune deletes the line — even though bundleDefsForOrders() would still have priced it. Needs either a server change (/api/bundles/ also returning sets a cart references) or a decision from the owner about whether hiding a set should empty the baskets that hold it. Not a small change, and the prune it would alter is the code that protects checkout.

[r21/products] #9 the goods editor toasts «Сохранено ✓» before the server answers and never rolls back. Real in the sense that there is no rollback and no refetch — though srvSaved() does toast «Не удалось сохранить на сервере» and «Нужен вход в админку» on a failure, so it is not silent. Making it roll back means changing the panel's whole optimistic write + journal model (demoApply → localStorage → srvPush); that is an architecture decision, not a patch.

[r21/products] #10 a lost answer on «Сохранить товар» lets the retry create a duplicate live product. Real: customCreate's catch releases S.goodsBusy and invites the retry, POST /api/admin/products has no idempotency key, and uniqueCustomId hands the second one <base>-2. Needs a server-side idempotency key (or a brand+name guard) in src/lib/custom-products.ts plus a column; a client-only patch would still race. Both products are visible in the list and deletable, so the damage is recoverable.

[r21/products] #11 renaming a volume and typing its stock in one save can land the count on a ghost variant. Real: the qty loop fires stock_adjust against the OLD label baked into data-edqty while customUpdate's PUT goes out in the same tick, and syncVariantRows() renames the row — if the PUT lands first the adjust re-creates the old label as a fresh, wrong row. This is the highest-value one left. The fix (await the product PUT, then post the moves keyed on the NEW label) reorders the one handler that writes prices, overrides, the product row, stock moves and barcodes together; doing it safely wants its own pass, not a corner of this one.